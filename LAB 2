WRITE A PROGRAM TO REVERSE A NUMBER    n=123
#include <stdio.h>

int main() {
    int n = 123, rev = 0, rem;

    while (n != 0) {
        rem = n % 10;          
        rev = rev * 10 + rem; 
        n = n / 10;           
    }

    printf("Reversed number = %d", rev);

    return 0;
}
